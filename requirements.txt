# Healthcare Knowledge Graph - Dipendenze
# ====================================

# Framework per l'interfaccia web
streamlit==1.32.0  # Framework per la creazione di applicazioni web interattive

# Analisi dati e grafi
pandas==2.2.0      # Libreria per l'analisi e manipolazione dei dati
numpy==1.26.3      # Libreria per il calcolo numerico
networkx==3.2.1    # Libreria per la creazione e manipolazione di grafi

# Visualizzazione
plotly==5.18.0     # Libreria per la creazione di visualizzazioni interattive

# Web framework e HTTP
flask==2.3.3       # Framework web per API
requests==2.31.0   # Libreria per richieste HTTP

# Analisi NLP con AI
openai==1.12.0     # Libreria per l'accesso alle API di OpenAI (GPT-4)
google-generativeai==0.3.2  # Libreria per l'accesso alle API di Google Gemini

# Gestione configurazione
python-dotenv==1.0.0  # Libreria per gestire le variabili d'ambiente


